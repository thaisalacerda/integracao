source 'https://rubygems.org'

raise 'Ruby version should be 2.0 or higher' unless RUBY_VERSION.to_f >= 2.0

gem 'jekyll', '~> 3.1.1'
gem 'posix-spawn'
gem 'redcarpet'

group :test do
  gem 'rake'
  gem 'html-proofer'
end
